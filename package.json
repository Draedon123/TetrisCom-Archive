{
  "name": "tetris",
  "version": "0.0.0",
  "scripts": {
    "format": "prettier . -w",
    "deploy": "git subtree push --prefix src origin gh-pages"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
